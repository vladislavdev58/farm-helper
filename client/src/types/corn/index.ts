export type TypeCornData = {
    __v: number
    _id: string
    cost: number
    owner: string
    name: string
    weight: number
    color: string[]
}

export type TypeCornForm = {
    name: string
    weight: number
    cost: number
}